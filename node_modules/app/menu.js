const { RichEmbed } = require("discord.js");

const MENU = [
	"Mac And Cheese",
	"Chicken Nuggets",
];

module.exports = function(user) {
	new RichEmbed()
		.setColor("RED")
		.setTitle("Menu")
		.setFooter(`Max: 3 | Ran by ${user.tag}`)
		.setDescription(MENU.join("\n"));
}
