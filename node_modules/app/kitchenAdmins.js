module.exports = function kitchenAdmin(member) {
	var kitchenAdmin = (member.highestRole.position >= member.guild.roles.find("name", "Management Team").position) ? true : false;
	return kitchenAdmin;
}
