var report = require("app/report");

process.on("unhandledRejection", (reason, line) => {
	report(new Error(line, reason));
});
process.on("warning", (warning) => {
	report(new Error(warning));
})
module.exports = function handle(client) {
	client.on("error", report);
}
